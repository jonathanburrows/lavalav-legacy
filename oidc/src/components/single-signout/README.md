# Single Signout Component

## Goals
Provide a page which will log the user out of other tabs, so single signout is supported.


## Remarks
This shouldnt ever be rendered as a component. It has a route /oidc/single-signout